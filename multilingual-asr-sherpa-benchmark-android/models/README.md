# Model binaries are downloaded at build time.

Official archive:

https://github.com/k2-fsa/sherpa-onnx/releases/download/asr-models/sherpa-onnx-zipformer-vi-30M-int8-2026-02-09.tar.bz2

Do not commit encoder/decoder/joiner/tokens/bpe binaries to git.
See `deps.lock` for SHA256 pins and `scripts/prepare-deps.sh`.
