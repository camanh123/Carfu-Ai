# Downloaded at build by scripts/prepare-deps.sh

- `sherpa-onnx-android/` official v1.13.8 arm64-v8a JNI + ONNX Runtime `.so`
- `sherpa-onnx-kotlin-api/` official Kotlin JNI wrappers from the same tag

Do not commit those trees. Pins live in `deps.lock`.
