org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
android.nonFinalResIds=true
android.injected.build.abi=arm64-v8a
# AGP injects android:testOnly="true" into debug manifests. That blocks
# ordinary Package Installer sideload (INSTALL_ALLOW_TEST is not supplied).
# This diagnostic APK must be tap-installable on CARFU Android 10.
android.injected.testOnly=false
