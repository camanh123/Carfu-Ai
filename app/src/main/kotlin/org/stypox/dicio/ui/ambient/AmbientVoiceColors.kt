package org.stypox.dicio.ui.ambient

import androidx.compose.ui.graphics.Color

/** Ice Cyan identity for Ambient Voice (listening). Soft HUD, not neon gaming. */
object AmbientVoiceColors {
    val CenterBright = Color(0xFFE8FFFF)
    val WaveMain = Color(0xFF59E6FF)
    val Glow = Color(0xFF00C8FF)
    val Transcript = Color(0xFFF2FFFF)
    val TranscriptShadow = Color(0x6600C8FF)
}
